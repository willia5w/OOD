package edu.neu.khoury.cs5004.problem1;

import com.sun.javaws.exceptions.InvalidArgumentException;
import sun.tools.jar.CommandLine;

public class HomebakeProcessor {


  // operation String that Homebake should execute. Should not surpress warnings.
  // Flag should be an Object


  // Program should terminate if none of the 3 below operations are called

  // install - an argument that initiates install operation on Homebake
  // uninstall - an argument that initiates uninstall operation on Homebake
  // update - an argument that initiates update operation on Homebake

  // formula - a String, representing the name of the software we want to install (REQUIRED)

  // warningsFlag - a String, with value -q, representing the flag to denote that any
  //and all warnings that may arise during installation should be suppressed. (OPTIONAL)

  // verboseFlag -a String, with value -v, representing the flag to denote that the
  //operation should be executed in a verbose manner. (OPTIONAL, NOT DEFAULT)


  // debugFlag -a String, with value -d, representing the flag to denote that the
  //operation should be executed by displaying any debugging information. (OPTIONAL, NOT DEFAULT)

  // WHEN PROCESSING:

  // Arguments VALID: Return new HomebakeData instance
  // Arguments INVALID: Throw new InvalidArgumentExeception.

  public static void main(String[] args) throws InvalidArgumentException {



}
